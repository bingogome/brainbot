# Generic teleop server template
teleop:
  mode: local
  config:
    type: bi_so101_leader
    id: leader
    left_arm_port: /dev/ttyACM1
    right_arm_port: /dev/ttyACM0
network:
  host: 0.0.0.0
  port: 7001
