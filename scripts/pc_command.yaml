# Brainbot command service configuration for operator PC
teleops:
  leader:
    type: bi_so101_leader
    id: leader
    left_arm_port: /dev/ttyACM1
    right_arm_port: /dev/ttyACM0
  # ar:
  #   type: ar_controller   # replace with your AR teleop class
  #   id: quest3

ai:
  host: 192.168.1.40      # Jetson Thor IP hosting GR00T
  port: 6000
  timeout_ms: 2000
  instruction_key: language_instruction

network:
  host: 0.0.0.0
  port: 5555
default_mode: idle
metadata:
  operator: workstation
